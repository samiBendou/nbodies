pub enum Distance {
    Meter,
    Astronomic,
    Light,
}

pub enum Time {
    Second,
    Calendar,
    Light,
}

pub enum Mass {
    Grams,
    Tons,
}

pub enum Angle {
    Radians,
    Degrees,
}