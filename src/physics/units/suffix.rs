pub enum Distance {
    Meter,
    Astronomic,
    Light,
    Pixel,
}

pub enum Time {
    Second,
    Calendar,
    Light,
}

pub enum Mass {
    Grams,
    Kilograms,
    Tons,
}

pub enum Angle {
    Radians,
    Degrees,
}